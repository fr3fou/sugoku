name: Test and Lint
on: [push, pull_request]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Install Go
        uses: actions/setup-go@v1
        with:
          go-version: 1.13.x
      - uses: actions/checkout@v1
      - name: Install golangci-lint
        run: |
          go get github.com/golangci/golangci-lint/cmd/golangci-lint
      - name: "Install sdl"
        run: "sudo apt install libsdl2{,-image,-mixer,-ttf,-gfx}-dev"
      - name: Run linters
        run: |
          export PATH=$PATH:$(go env GOPATH)/bin   
          golangci-lint -E bodyclose,misspell,gocyclo,gofmt,golint,unconvert,goimports,depguard,gocritic,interfacer run
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Install Go
        uses: actions/setup-go@v1
        with:
          go-version: 1.13.x
      - uses: actions/checkout@v1
      - name: "Install sdl"
        run: "apt install libsdl2{,-image,-mixer,-ttf,-gfx}-dev"
      - name: Run tests
        run: go test -v -covermode=atomic -race ./...
